// Re-export everything from the shared package
export { supabase, getCurrentUser } from '@budlife/shared'
export type {
  Product,
  ProductVariant,
  ProductImage,
  DashboardMetrics,
  Customer,
  Order,
} from '@budlife/shared'
